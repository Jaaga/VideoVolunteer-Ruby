- arr = global_arr_set.push('district', 'mentor', 'note').sort
- dates = global_dates_set.push('created_at').sort
- defaults = ['cc_name', 'state', 'story_pitch_date',
              'flag', 'flag_date', 'flag_notes', 'updated_at']
/ x=1 to take into account displaying the UID seperately.
- x = 1
- count = defaults.length + x
- final_count = count + arr.length

%h1= @title

.checks
  %p
    %span.glyphicon.glyphicon-th-list
    Change Columns
  %ul.hide-list
    %li
      %input.0{ type: 'checkbox', checked: true } UID
    = results_check(defaults, x, true)
    = results_check(arr, count)
    = results_check(dates, final_count)

.table-responsive
  %table.table.table-bordered.table-striped
    %thead
      %tr
        %th.col0 UID
        = results_head(defaults, x)
        = results_head(arr, count)
        = results_head(dates, final_count)
    %tbody
      - @track.each do |t|
        %tr
          %td.col0
            %a{ href: "/show/#{ t.uid }" }
              =h t.uid
          = results_body(defaults, x, t)
          = results_body(arr, count, t)
          = results_body(dates, final_count, t)
